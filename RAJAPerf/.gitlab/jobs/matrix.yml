##############################################################################
# Copyright (c) Lawrence Livermore National Security, LLC and other
# RAJA Project Developers. See top-level LICENSE and COPYRIGHT
# files for dates and other details. No copyright assignment is required
# to contribute to RAJA Performance Suite.
#
# SPDX-License-Identifier: (BSD-3-Clause)
##############################################################################

# Override reproducer section to define projet specific variables.
.matrix_reproducer_vars:
  script:
    - !reference [.reproducer_vars, script]

########################
# Overridden shared jobs
########################
# We duplicate the shared jobs description and add necessary changes for RAJA.
# We keep ${PROJECT_<MACHINE>_VARIANTS} and ${PROJECT_<MACHINE>_DEPS} So that
# the comparison with the original job is easier.

# Shared job uses %clang-18-gcc-13, replaced with %clang-18 below
clang_18_1_1_gcc_13_cuda_12_6_0:
  extends: .job_on_matrix
  variables:
    ON_MATRIX: "OFF"

############
# Extra jobs
############
# We do not recommend using ${PROJECT_<MACHINE>_VARIANTS} and
# ${PROJECT_<MACHINE>_DEPS} in the extra jobs. There is no reason not to fully
# describe the spec here.

# clang_18_1_8_cuda_12_6_0
clang_18_1_8_cuda_12_6_0:
  variables:
    SPEC: " ~shared +openmp +cuda cuda_arch=90 %llvm@=18.1.8 ^cuda@12.6.0+allow-unsupported-compilers"
  extends: .job_on_matrix
  allow_failure: true
