from .wintap import wintap_pipeline

# TODO: add all pipelines that should be exposed to the user of your backend in the import statement above.

pipelines = {
    "wintap_pipeline": wintap_pipeline,  # TODO: adapt identifier to something approproiate
}
