# Development

- Setuptools
- VirtualEnv

``` shell
virtualenv venv
. venv/bin/activate
pip install --editable .
```

Then run the commands:

``` shell
scr
scr config
scr --help
scr --version
```


## Highlevel TODOs

1. Testing
2. Automatic documentation generation
